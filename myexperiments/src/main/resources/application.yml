security:
  user:
    name: buzz
    password: infinity

pizza:
  orders:
    pageSize: 10


  discount:
    codes:
      abcdef: 10
      
#logging:
#  level:
#    org.springframework.data.rest: debug

# Having added the spring-boot-starter-data-rest to the application classpath, one gets preconfigured REST endpoints
# for all the APIs generated by the spring-data project. As these APIs can overlap with those defined by the developers
# Spring gives the chance to prepend a prefix to the preconfigured APIs
spring:
  data:
    rest:
      base-path: /api

---
spring:
  profiles: prod
  
  datasource:
    url: jdbc:mysql://localhost/pizzacloud
    username: pizzauser
    password: pizzapassword
    
logging:
  level:
    pizzas: WARN
    
