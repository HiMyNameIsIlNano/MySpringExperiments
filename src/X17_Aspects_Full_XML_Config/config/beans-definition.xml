<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:aop="http://www.springframework.org/schema/aop"
       xsi:schemaLocation="http://www.springframework.org/schema/aop
       http://www.springframework.org/schema/aop/spring-aop.xsd
       http://www.springframework.org/schema/beans
       http://www.springframework.org/schema/beans/spring-beans.xsd
       http://www.springframework.org/schema/context
       http://www.springframework.org/schema/context/spring-context.xsd">

    <context:component-scan base-package="X17_Aspects_Full_XML_Config"/>

    <aop:aspectj-autoproxy/>

    <bean id="myFullXMLConfiguredAspectX17" class="X17_Aspects_Full_XML_Config.aspect.MyAspect"/>

    <aop:config>
        <!-- This is a global pointcut. Moving this inside the aspect would not work. It seems as if an Aspect can only
         declare one pointcut -->
        <aop:pointcut id="myPointcutForImprovement"
                      expression="execution(* X17_Aspects_Full_XML_Config.service.DummyService.aMethodIWantToImprove(..)))"/>

        <aop:aspect ref="myFullXMLConfiguredAspectX17">
            <!-- This is a pointcut local to the advice -->
            <aop:pointcut id="myPointcut"
                          expression="execution(* X17_Aspects_Full_XML_Config.service.DummyService.aMethodIWantToTest(..)))"/>

            <aop:pointcut id="myPointcutForImprovement"
                          expression="execution(* X17_Aspects_Full_XML_Config.service.DummyService.aMethodIWantToImprove(..)))"/>

            <aop:before pointcut-ref="myPointcut"
                        method="logBefore"/>

            <aop:after pointcut-ref="myPointcut"
                       method="logAfter"/>

            <aop:around pointcut-ref="myPointcutForImprovement"
                        method="logAround"/>
        </aop:aspect>

    </aop:config>

</beans>